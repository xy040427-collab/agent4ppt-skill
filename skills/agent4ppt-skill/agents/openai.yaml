interface:
  display_name: "Agent4PPT"
  short_description: "Full-slide generation and visually matched editable PPT"
  default_prompt: "Use $agent4ppt-skill to generate complete slides, then create editable versions with image-model text removal, PowerPoint MCP restoration and visual comparison."

policy:
  allow_implicit_invocation: true
